# Leave unset for a standalone frontend-only deploy (Stage A) — set once the
# backend is deployed (Stage B) to switch content sections to live data.
VITE_API_BASE_URL=
VITE_WEB3FORMS_KEY=
VITE_TURNSTILE_SITE_KEY=